#!/bin/sh
# PLACEHOLDER — not a real kernel module
#
# Copy the actual infutil.ko from the Infineon NCP driver package drop into:
#   som-external/if91x/package/summit-infhosted-backports/files/infutil.ko
# before building.
echo "ERROR: infutil.ko is a placeholder. Replace with the actual NCP .ko from the Infineon package drop." >&2
exit 1
