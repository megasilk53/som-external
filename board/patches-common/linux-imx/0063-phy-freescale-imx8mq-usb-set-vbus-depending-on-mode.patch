From c88667a226ef54b656dc16f0f3c52897c0c76605 Mon Sep 17 00:00:00 2001
From: Gary Bisson <gary.bisson@ezurio.com>
Date: Tue, 7 May 2024 14:22:36 +0200
Subject: [PATCH] phy: freescale: imx8mq-usb: set vbus depending on mode

Signed-off-by: Gary Bisson <gary.bisson@ezurio.com>
---
 drivers/phy/freescale/phy-fsl-imx8mq-usb.c | 22 ++++++++++++++++------
 1 file changed, 16 insertions(+), 6 deletions(-)

Upstream-Status: Inappropriate No time for upstreaming 

diff --git a/drivers/phy/freescale/phy-fsl-imx8mq-usb.c b/drivers/phy/freescale/phy-fsl-imx8mq-usb.c
index 257e8f3f3c7e..72ae1a99e83b 100644
--- a/drivers/phy/freescale/phy-fsl-imx8mq-usb.c
+++ b/drivers/phy/freescale/phy-fsl-imx8mq-usb.c
@@ -773,9 +773,11 @@ static int imx8mq_phy_power_on(struct phy *phy)
 	u32 value;
 	int ret;
 
-	ret = regulator_enable(imx_phy->vbus);
-	if (ret)
-		return ret;
+	if (!regulator_is_enabled(imx_phy->vbus)) {
+		ret = regulator_enable(imx_phy->vbus);
+		if (ret)
+			return ret;
+	}
 
 	ret = clk_prepare_enable(imx_phy->clk);
 	if (ret)
@@ -800,7 +802,8 @@ static int imx8mq_phy_power_off(struct phy *phy)
 
 	clk_disable_unprepare(imx_phy->alt_clk);
 	clk_disable_unprepare(imx_phy->clk);
-	regulator_disable(imx_phy->vbus);
+	if (regulator_is_enabled(imx_phy->vbus))
+		regulator_disable(imx_phy->vbus);
 
 	return 0;
 }
@@ -1032,11 +1035,18 @@ static int imx8mq_phy_set_mode(struct phy *phy, enum phy_mode mode,
 			       int submode)
 {
 	struct imx8mq_usb_phy *imx_phy = phy_get_drvdata(phy);
+	int ret = 0;
 
-	if (mode == PHY_MODE_USB_DEVICE)
+	if (mode == PHY_MODE_USB_DEVICE) {
+		if (regulator_is_enabled(imx_phy->vbus))
+			ret = regulator_disable(imx_phy->vbus);
 		return imx8mq_phy_charger_detect(imx_phy);
+	} else if (mode == PHY_MODE_USB_HOST) {
+		if (!regulator_is_enabled(imx_phy->vbus))
+			ret = regulator_enable(imx_phy->vbus);
+	}
 
-	return 0;
+	return ret;
 }
 
 static const struct phy_ops imx8mq_usb_phy_ops = {
-- 
2.53.0
